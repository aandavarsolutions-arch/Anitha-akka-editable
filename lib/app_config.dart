
class AppConfig {
  /// Toggle this to TRUE to create a 'Client Copy' (Read-Only).
  /// Toggle this to FALSE for the 'User Copy' (Full Edit).
  static const bool isReadOnly = false;
  
  // You can add other global configuration flags here if needed.
}
