Jarvis v1 Fixed - Flutter Windows starter (FFI DB)

Extract this ZIP to:
D:\CONSTRUCTION\jarvis_v1_fixed

Then run:
  cd D:\CONSTRUCTION\jarvis_v1_fixed
  flutter pub get
  flutter run -d windows

Notes:
- This ZIP includes all app source files and basic Windows runner CMake files.
- If you see any package version issues, edit pubspec.yaml to use compatible versions for your Flutter SDK.
